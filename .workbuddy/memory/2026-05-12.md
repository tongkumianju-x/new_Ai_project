# 2026-05-12

## mdns-scanner 工程初始化

- 在 `feature/mdns-scanner-init` 分支搭建 mDNS 资产测绘 CLI（Go 标准库）
- 工程结构：`cmd/mdnsscan` + `cmd/mdnsreplay` + `internal/{mdns,scanner,output}` + `pkg/iprange`
- 关键设计：
  - 自实现 mDNS 编解码（Header/Name 压缩/PTR/SRV/TXT/A/AAAA）；不依赖 miekg/dns
  - 单播探测每个目标 IP 的 5353 + class 高位置位（unicast-response）；附加可选组播探测
  - `MergeMessages` 公开导出，便于外部测试与离线回放工具使用（避免 internal 包测试受限）
  - 输出文本与示例 100% 对齐（`9/tcp workstation`、`Name=`、`IPv4=`、`IPv6=`、`Hostname=`、`TTL=`、TXT 一行 key=val,...）
- 测试：`go test ./...` 全绿；本机真实扫描 `127.0.0.1` 可识别 AirPlay/RAOP/Companion-Link 服务
- 提交：`e75f52f feat(mdns-scanner): bootstrap mDNS asset scanning CLI`
- 后续递进路线写入 README，按分支推进（multicast-iface / fingerprint / rate-limit / output-csv-prom / e2e）

## 阶段2: feature/mdns-fingerprint 已合并 main

- 完成 mDNS 厂商指纹库（internal/fingerprint）
- 内置 QNAP/Synology/Apple/HomeKit/Chromecast/Spotify/Printer/Linux-Workstation 等规则
- 设计：scanner 通过 FingerprintHook 调用，依赖单向，便于业务注入自定义规则
- 测试：8 个用例全部通过（QNAP/Synology/Apple家族/优先级/未知/自定义注入/端到端）
- 提交：c67d2a5 -> Merge 24c9cc9，已 push origin main
- 注：环境无 gh / browser-use，无法用浏览器登录 GitHub 网页交互；已用本地 --no-ff 合并完整保留 review 记录

## 阶段3收尾: 实际运行验证 + bug修复

- 用 ./bin/mdnsreplay 真实运行 CLI（非测试集），输出与题目示例 expected.txt 做 diff，归一化后 0 行差异
- 真机扫描 127.0.0.1 发现 Apple Mac 3 个 Bonjour 服务，深度 banner 完整
- 真机运行发现 bug：answers.PTR 段泄漏了 _services._dns-sd._udp.local 元枚举
- 修复：merge.go 在 PTR 收集环节直接过滤；新增 TestRegression_NoMetaServicesInAnswers 防回归
- docs/RUN_VERIFICATION.md 完整对照报告作为交付文档
- testdata/ 留档：expected.txt / actual.txt / actual.normalized.txt / real-loopback.{txt,log,json} / real-lan.{txt,log}
- 提交 fix/meta-ptr-leak -> 合并 main (4c1a671)，已 push origin

## 最终推送

- main 已推送到 origin: 4c1a671 (Merge: fix/meta-ptr-leak -> main)
- 本地 SHA == 远端 SHA，完全对齐
- 远端可访问: https://github.com/tongkumianju-x/new_Ai_project
